services:
  # MongoDB
  mongodb:
    image: mongo:noble
    container_name: ataccoteque-mongo
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_INITDB_ROOT_USERNAME:-ataccoteque_user}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_INITDB_ROOT_PASSWORD:-userpassword}
      MONGO_DB_NAME: ${MONGO_DB_NAME:-ataccoteque}
    volumes:
      - mongo_data:/data/db
    networks:
      - ataccoteque-network

  postgresql:
    image: postgres:latest
    container_name: ataccoteque-postgresql
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgresql_data:/var/lib/postgresql
    networks:
      - ataccoteque-network

  # Meilisearch
  meilisearch:
    image: getmeili/meilisearch:v1.6
    container_name: ataccoteque-meilisearch
    environment:
      MEILI_MASTER_KEY: ${MEILI_MASTER_KEY:-masterKey123}
      MEILI_ENV: ${MEILI_ENV:-production}
    ports:
      - "7700:7700"
    volumes:
      - meilisearch_data:/meili_data
    networks:
      - ataccoteque-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:7700/health"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Backend
  backend:
    build:
      context: ./app/server
      dockerfile: Dockerfile
    container_name: ataccoteque-backend
    environment:
      NODE_ENV: production
      PORT: 3000
      MONGO_URI: mongodb://${MONGO_INITDB_ROOT_USERNAME}:${MONGO_INITDB_ROOT_PASSWORD}@mongodb:27017/${MONGO_DB_NAME:-ataccoteque}?authSource=admin
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgresql:5432/${POSTGRES_DB}?schema=public
      MEILI_HOST: http://meilisearch:7700
      MEILI_MASTER_KEY: ${MEILI_MASTER_KEY:-masterKey123}
      JWT_SECRET: ${JWT_SECRET:-changeMe}
    depends_on:
      meilisearch:
        condition: service_healthy
    networks:
      - ataccoteque-network

  # Frontend
  frontend:
    build:
      context: ./app/client
      dockerfile: Dockerfile
      args:
        VITE_API_ENDPOINT: ${API_ENDPOINT:-/api}
    container_name: ataccoteque-frontend
    depends_on:
      - backend
    networks:
      - ataccoteque-network

  # Nginx reverse proxy
  nginx:
    image: nginx:alpine
    container_name: ataccoteque-nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./files:/var/www/files:ro
    depends_on:
      - frontend
      - backend
    networks:
      - ataccoteque-network

volumes:
  mongo_data:
  meilisearch_data:
  postgresql_data:

networks:
  ataccoteque-network:
    driver: bridge
